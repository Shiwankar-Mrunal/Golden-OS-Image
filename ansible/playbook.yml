
- name: Base system setup
  hosts: all
  become: yes
  gather_facts: yes
  vars : 
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - OS Bootstrapping
    - System_Configuration
    - SSH_UserSecurity
    - Firewall_OS_Hardening
    - SystemPerformanceTuning
